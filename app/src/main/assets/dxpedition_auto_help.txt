远征 Hound 自动程序

开启后：
- FT8 远征复合消息会参与自动通联状态机。
- 收到 Fox 发给你的信号报告后，程序会自动推进到 Hound 的 R+rpt 发射。
- 收到 Fox 的 RR73 后，程序会自动完成本次通联并返回普通自动流程。

关闭后：
- 远征消息仍然会正常解码和显示。
- 自由消息模式、实验性解码模式不受影响。
- 你仍然可以手动发射远征消息，但自动程序不会替你推进远征回复序列。

这个开关只影响 FT8 远征 Hound 自动流程，不影响普通 FT8 / FT4 自动通联。
